# Streamlit Deployment Requirements (Cloud-optimized)
# This file contains ONLY the dependencies needed for cloud deployment
# Excludes heavy local ML libraries

# Core
streamlit==1.29.0
python-dotenv==1.0.0
pyyaml==6.0.1
pydantic==2.5.0

# LLM Providers (API-based only)
openai==1.6.1
anthropic==0.8.1

# LangChain (minimal)
langchain==0.1.0
langchain-community==0.0.10
langchain-openai==0.0.2
langchain-anthropic==0.0.1

# Vector Database (ChromaDB only for cloud)
chromadb==0.4.22

# Document Processing
pypdf==3.17.4
tiktoken==0.5.2

# Data Processing
pandas==2.1.4
numpy==1.26.2

# Visualization
plotly==5.18.0

# Utilities
requests==2.31.0
loguru==0.7.2
